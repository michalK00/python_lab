import sys
import os
import shutil

if __name__ == "__main__":
    # tworzenie zipa shutil.make_archive(output_filename, 'zip', dir_name)
    print(os.path.join(os.environ["HOMEPATH"], "Desktop"))
    # if len(sys.argv) == 2:
    #    sys.argv[1]
